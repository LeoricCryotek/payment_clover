<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="payment_clover.PaymentTerminal">
        <div class="o_action container-fluid py-4">
            <div class="row justify-content-center">
                <div class="col-lg-6 col-md-8">

                    <h2 class="mb-4">
                        <i class="fa fa-credit-card me-2"/>
                        Clover Payment Terminal
                        <t t-if="state.providers.length > 0">
                            <t t-set="activeProv"
                               t-value="state.providers.find(p => p.id === state.selectedProviderId)"/>
                            <span t-if="activeProv and activeProv.state === 'test'"
                                  class="badge bg-warning text-dark ms-2"
                                  style="font-size: 0.5em; vertical-align: middle;">
                                <i class="fa fa-flask me-1"/>SANDBOX
                            </span>
                        </t>
                    </h2>

                    <!-- ========== FORM STEP ========== -->
                    <div t-if="state.step === 'form'" class="card shadow-sm">
                        <div class="card-body">

                            <!-- Provider selector (if multiple) -->
                            <div t-if="state.providers.length > 1" class="mb-3">
                                <label class="form-label fw-bold">Payment Provider</label>
                                <select class="form-select"
                                        t-on-change="(ev) => this.state.selectedProviderId = parseInt(ev.target.value)">
                                    <option value="">-- Select --</option>
                                    <t t-foreach="state.providers" t-as="prov" t-key="prov.id">
                                        <option t-att-value="prov.id"
                                                t-att-selected="prov.id === state.selectedProviderId">
                                            <t t-esc="prov.name"/>
                                            <t t-if="prov.state === 'test'"> (TEST)</t>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            <div t-elif="state.providers.length === 1" class="mb-3">
                                <span class="badge bg-primary">
                                    <t t-esc="state.providers[0].name"/>
                                    <t t-if="state.providers[0].state === 'test'"> (TEST)</t>
                                </span>
                            </div>
                            <div t-elif="state.providers.length === 0" class="alert alert-warning">
                                No Clover payment provider is configured.
                                Please set one up under Payment Providers.
                            </div>

                            <!-- Customer search -->
                            <div class="mb-3 position-relative">
                                <label class="form-label fw-bold">Customer</label>
                                <div class="input-group">
                                    <input type="text" class="form-control"
                                           placeholder="Search by name, email, or phone..."
                                           t-model="state.partnerSearch"
                                           t-on-input="onSearchPartner"
                                           t-att-disabled="state.isGuest"/>
                                    <button type="button"
                                            class="btn btn-outline-secondary"
                                            t-att-class="state.isGuest ? 'btn btn-secondary' : 'btn btn-outline-secondary'"
                                            t-on-click="toggleGuest"
                                            title="Guest / Walk-in checkout">
                                        <i class="fa fa-user-secret me-1"/>Guest
                                    </button>
                                </div>
                                <div t-if="state.partners.length > 0 and !state.isGuest"
                                     class="list-group mt-1 position-absolute shadow bg-white"
                                     style="z-index: 1050; max-height: 200px; overflow-y: auto; left: 0; right: 0;">
                                    <t t-foreach="state.partners" t-as="p" t-key="p.id">
                                        <button type="button"
                                                class="list-group-item list-group-item-action"
                                                t-on-click="() => this.selectPartner(p.id)">
                                            <strong t-esc="p.name"/>
                                            <small t-if="p.email" class="text-muted ms-2"
                                                   t-esc="p.email"/>
                                        </button>
                                    </t>
                                </div>
                                <small t-if="state.isGuest" class="form-text text-muted">
                                    <i class="fa fa-info-circle me-1"/>Processing as guest walk-in.
                                </small>
                            </div>

                            <!-- Item picker (from synced Clover inventory) -->
                            <div t-if="state.cloverItems.length > 0" class="mb-3">
                                <label class="form-label fw-bold">Item (optional)</label>
                                <select class="form-select"
                                        t-on-change="selectItem">
                                    <option value="">-- Select an item --</option>
                                    <t t-foreach="state.cloverItems" t-as="item" t-key="item.id">
                                        <option t-att-value="item.id"
                                                t-att-selected="item.id === state.selectedItemId">
                                            <t t-if="item.category_name">
                                                [<t t-esc="item.category_name"/>]
                                            </t>
                                            <t t-esc="item.name"/>
                                            <t t-if="item.price_type === 'FIXED'">
                                                — $<t t-esc="item.price.toFixed(2)"/>
                                            </t>
                                            <t t-if="item.price_type === 'VARIABLE'">
                                                — (enter amount)
                                            </t>
                                        </option>
                                    </t>
                                </select>
                                <small class="form-text text-muted">
                                    Selecting an item auto-fills amount and description.
                                </small>
                            </div>
                            <div t-if="state.cloverItems.length === 0 and state.itemsLoading" class="mb-3">
                                <label class="form-label fw-bold">Item</label>
                                <div class="text-muted small">
                                    <i class="fa fa-spinner fa-spin me-1"/>Loading items...
                                </div>
                            </div>

                            <!-- Amount -->
                            <div class="mb-3">
                                <label class="form-label fw-bold">Amount (USD)</label>
                                <input type="number" class="form-control"
                                       step="0.01" min="0.01"
                                       placeholder="0.00"
                                       t-model="state.amount"/>
                            </div>

                            <!-- Description -->
                            <div class="mb-3">
                                <label class="form-label fw-bold">Description (optional)</label>
                                <input type="text" class="form-control"
                                       placeholder="e.g. Annual dues, Event ticket..."
                                       t-model="state.description"/>
                            </div>

                            <!-- Card input (Clover iframe) -->
                            <div class="mb-3 p-3 border rounded bg-light">
                                <h6 class="fw-bold mb-3">
                                    <i class="fa fa-lock me-1"/>
                                    Card Details
                                    <t t-if="state.cardReady">
                                        <span class="badge bg-success ms-2">
                                            <i class="fa fa-check me-1"/>Ready
                                        </span>
                                    </t>
                                    <t t-elif="state.cardError">
                                        <span class="badge bg-danger ms-2">
                                            <i class="fa fa-exclamation-triangle me-1"/>Error
                                        </span>
                                    </t>
                                    <t t-else="">
                                        <span class="badge bg-secondary ms-2">
                                            <i class="fa fa-spinner fa-spin me-1"/>Loading...
                                        </span>
                                    </t>
                                </h6>
                                <t t-if="state.cardError">
                                    <div class="alert alert-danger small mb-2">
                                        <t t-esc="state.cardError"/>
                                    </div>
                                </t>
                                <div class="mb-2">
                                    <label class="form-label small">Card Number</label>
                                    <div id="terminal-card-number" class="form-control"
                                         style="height: 40px; padding-top: 10px;"></div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-5">
                                        <label class="form-label small">Exp Date</label>
                                        <div id="terminal-card-date" class="form-control"
                                             style="height: 40px; padding-top: 10px;"></div>
                                    </div>
                                    <div class="col-3">
                                        <label class="form-label small">CVV</label>
                                        <div id="terminal-card-cvv" class="form-control"
                                             style="height: 40px; padding-top: 10px;"></div>
                                    </div>
                                    <div class="col-4">
                                        <label class="form-label small">ZIP</label>
                                        <div id="terminal-card-postal" class="form-control"
                                             style="height: 40px; padding-top: 10px;"></div>
                                    </div>
                                </div>
                                <button type="button"
                                        class="btn btn-outline-secondary btn-sm mt-1"
                                        t-on-click="initCloverIframe">
                                    <i class="fa fa-refresh me-1"/>
                                    Re-initialize Card Reader
                                </button>
                            </div>

                            <!-- Submit -->
                            <button type="button"
                                    class="btn btn-primary btn-lg w-100"
                                    t-att-disabled="!state.cardReady"
                                    t-on-click="processPayment">
                                <i class="fa fa-bolt me-2"/>
                                Process Payment
                            </button>

                            <!-- Test-mode helper -->
                            <t t-if="state.providers.length > 0">
                                <t t-set="selProv"
                                   t-value="state.providers.find(p => p.id === state.selectedProviderId)"/>
                                <div t-if="selProv and selProv.state === 'test'"
                                     class="alert alert-info mt-3 small mb-0">
                                    <i class="fa fa-flask me-1"/>
                                    <strong>Sandbox Mode</strong> — use these test cards:
                                    <code>4111 1111 1111 1111</code> (Visa),
                                    <code>5500 0000 0000 0004</code> (MC),
                                    <code>6011 1111 1111 1117</code> (Discover).
                                    Exp: any future date. CVV: any 3 digits.
                                    ZIP: any 5 digits.
                                    <br/>
                                    <em>If tokenization times out, verify that
                                    your PAKMS key was obtained from the
                                    <strong>sandbox</strong> Clover dashboard
                                    (sandbox.dev.clover.com), not production.</em>
                                </div>
                            </t>
                        </div>
                    </div>

                    <!-- ========== PROCESSING STEP ========== -->
                    <div t-if="state.step === 'processing'" class="card shadow-sm">
                        <div class="card-body text-center py-5">
                            <div class="spinner-border text-primary mb-3" role="status">
                                <span class="visually-hidden">Processing...</span>
                            </div>
                            <h4>Processing Payment...</h4>
                            <p class="text-muted">Please wait while the transaction completes.</p>
                            <p class="text-muted small">This should take no more than 30 seconds.</p>
                        </div>
                    </div>

                    <!-- ========== RESULT STEP ========== -->
                    <div t-if="state.step === 'result'" class="card shadow-sm">
                        <div class="card-body text-center py-5">
                            <div t-if="state.resultStatus === 'ok'">
                                <i class="fa fa-check-circle text-success"
                                   style="font-size: 4rem;"/>
                                <h3 class="mt-3 text-success">Payment Approved</h3>
                            </div>
                            <div t-else="">
                                <i class="fa fa-times-circle text-danger"
                                   style="font-size: 4rem;"/>
                                <h3 class="mt-3 text-danger">Payment Failed</h3>
                            </div>
                            <p class="mt-2" t-esc="state.resultMessage"/>
                            <p t-if="state.resultReference" class="text-muted small">
                                Reference: <t t-esc="state.resultReference"/>
                            </p>
                            <button type="button"
                                    class="btn btn-primary mt-3"
                                    t-on-click="resetForm">
                                <i class="fa fa-plus me-1"/>
                                New Payment
                            </button>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </t>

</templates>
